const joinNames = (namesObj) => {
  [ {name: 'Bart'},
    {name: 'Lisa'},
    {name: 'Maggie'}]

  let newString = ''

  for(let i = 0; i < namesObj.length; i++);
    newString += name.Obj[i].name + ","

}
  return namesObj

module.exports = joinNames;
